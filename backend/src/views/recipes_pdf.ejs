<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Мои рецепты</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .recipe { margin-bottom: 40px; page-break-inside: avoid; }
        .recipe h2 { border-bottom: 2px solid #333; padding-bottom: 5px; }
        .ingredients { margin-top: 10px; }
        .ingredient { margin-bottom: 5px; }
    </style>
</head>
<body>
<% recipes.forEach(recipe => { %>
    <div class="recipe">
        <h2><%= recipe.name %></h2>
        <p><strong>Описание:</strong> <%= recipe.instructions %></p>
        <% if (recipe.cookingTime) { %>
            <p><strong>Время готовки:</strong> <%= recipe.cookingTime.name %></p>
        <% } %>
        <div class="ingredients">
            <h3>Ингредиенты:</h3>
            <% if (recipe.ingredients && recipe.ingredients.length > 0) { %>
                <ul>
                    <% recipe.ingredients.forEach(ing => { %>
                        <li class="ingredient">
                            <%= ing.name %> — <%= ing.RecipesIngredients.quantity %> <%= ing.RecipesIngredients.unitName %>
                        </li>
                    <% }) %>
                </ul>
            <% } else { %>
                <p>Нет ингредиентов.</p>
            <% } %>
        </div>
    </div>
<% }) %>
</body>
</html>
